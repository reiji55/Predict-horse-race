# 未決事項リスト（実装前に判断が要るもの）

引き継ぎ書v1〜v7と各仕様書に散在している「未確定」「要確認」「未反映パッチ」を1箇所に集約したもの。
判断が済んだらこの表を更新し、該当仕様書にも反映すること。

出典表記：`v3§3` = 引き継ぎ書v3 §3、`買い目§5.5` = 買い目生成仕様 §5.5、など。

---

## A. 仕様書に未反映の決定 → ✅ 全件解消（2026-09-17）

| # | 対象 | 内容 | 状態 | 出典 |
|---|---|---|---|---|
| A-1 | データスキーマ races[] | `myomi_parts`（オプション）追加：`{umami, conf}` | ✅**反映済**。`データスキーマ仕様_v1.2.md` として発行（§1・§3・§6.6）。`predictions.sample.json` にも追加 | v3§3-1 / v6§6 |
| A-2 | 妙味メーター §7 #4・#5 | 頭数は入れない／`myomi_parts` は持つ | ✅**反映済**。「要相談」→「確定」に更新し、理由も本文に記載 | v3§2.1 / v6§6 |
| A-3 | 引き継ぎ書v1 §5 の④合成 | ④オッズ乖離は base_score に入れない（v3§2.2で上書き） | ✅**対応済**。v1は歴史的資料として据え置き、冒頭に「§5はv3で上書き済み」の注記を追加 | v3§3-3 |

> コードが参照していた `データスキーマ仕様_v1_2.md` / `妙味メーター仕様_v1_確定版.md` という
> 存在しないファイル名の問題も、A-1・A-2 の反映と参照名の統一で解消済み。

---

## B. 実装の判断が要る設計論点

**凡例**：🔵＝logic実装時に判断を入れた（configで切り替え可能。後方検証で見直す前提）／⬜＝未決。

| # | 論点 | 内容 | 状態 | 出典 |
|---|---|---|---|---|
| B-1 | **スピード指数のクラス補正の向き** | `expected_time = base_time + class_sec` により「指数100＝自分のクラスのpar」になり、§1の「指数100＝OPクラス勝ち馬水準」という定義と食い違う。結果、**下級条件を遅く走った馬が上級条件を速く走った馬より高い指数になる**（京都芝1200・base 67.8 で、未勝利69.5秒＝96.7 > OP 68.3秒＝91.7）。§1.4の検算はOP戦のためこの項を検証していない | 🔵**`class_credit` を新設（既定 0.0＝クラス補正を足し戻さない＝絶対時計で評価）**。`1.0` にすれば仕様書§1の記述そのままに戻る。どちらが回収率に効くかは後方検証で決める。`tests/test_logic.py::test_speed_class_credit_controls_class_handicap` が両方の挙動を固定している | 速度§1・§4 |
| B-2 | **メインレースの絞り込みルール** | `a_race_list.py` は「絞り込みなし・全レース対象」、`build_raw.py` のTODOは「絞り込みルール未確定」と食い違う。仕様側は一貫して「メインのみ」（取得項目§2.2＝土日のメイン4〜6、速度§0＝「現在の取得設計（メインレースのみ）」） | 🔵**A単体は絞り込み無し・絞り込みは build_raw の責務**と整理し、`config/scraper.json` の `main_race` で切り替えられるようにした（`race_no`（既定・各場11R）/ `grade` / `all`）。既定の11Rは3場開催の土日で6レースとなり、§2.2の「4〜6」と一致する。**「メイン」の定義そのものは運用者の確認待ち**（例：メインが10Rや12Rの日をどう扱うか） | 取得§1.1・§2.2 / 速度§0 |
| B-3 | **騎手リーディングの場別スコープ** | 取得項目§2.6 は当初 `scope="venue"`（当該場成績）と定めていた | 🔵**場別は存在しないことが確定**（sp版の絞り込みは 全国/関東/関西＝**所属**・年・並び順のみ。PC版 `?pid=jockey_leading` は実体なし）。よって **騎手も `scope="overall"` に改訂**し、取得項目仕様を v1.1 として発行した。`venue` フィールドは廃止。場別が欲しくなったら騎手個別ページ（出走者ぶんだけ）を取る案が将来候補だが、§1.2 が却下した個別ページ方式なのでページ数とのトレードオフを再検討すること | v7§2.4 |
| B-4 | **`amt` の単位** | 買い目§5.5 不変条件#1は「すべて100円単位」だが、`predictions.sample.json` と `results.sample.json` の哲さんカードに `amt: 150` がある（Σ=500 なので#2は満たす） | 🔵**`config/cards.json` の `amt_unit` を新設（既定 50）**。500円を厚み付きで配分する自由度を優先し、サンプルの表現を正とした。`100` に戻せば仕様の記述どおり | 買い目§5.5 |
| B-5 | **`marks[]` の範囲** | 買い目§2 は「7位以下は無印（marksに載せるが mk 空）」＝全出走馬。サンプルの marks は上位5頭のみ | 🔵**仕様の記述どおり全出走馬を載せる**（7位以下は `mk: ""`）。データスキーマ§3が「marks の score と odds は後方検証のために残す」と定めており、上位数頭だけでは重みの再計算ができないため。UIは `mk` が空の行を描画しなければよい | 買い目§2・§5.5 |
| B-6 | **softmax温度Tとスコアのスケール** | 妙味§1 の T=10.0 は、§4の検算（score 60〜90）と `marks[].score`（81.2 等）が示すとおり**0〜100スケールのスコア前提の値**。一方 base_score は z の加重和（概ね ±2）なので、そのまま T=10 で割ると全馬ほぼ横並びの p になり妙味が常時振り切れる。妙味§7 #2 が「Tは仮値、スコアスケール確定後に較正」と予告していた箇所 | 🔵**Tを動かさず、z を表示スケールに移すアフィン変換を新設**：`score = score_scale.base + score_scale.unit × base_score`（既定 70 + 10×z）。`score_unit`(10) と T(10) が等しいので実効的には「z空間で温度1.0」。18頭立てなら1番人気の p は25%前後に収まる | 妙味§1・§7#2 |
| B-9 | **キャラの予想が全員似通う／鳳を降臨させる意味が無い**（★2026-09-19 の初実戦で表面化） | λ（妙味寄せ係数）しか違わないので、4人とも**同じ実力順・同じ歪み尺度（p−q）**を見る。結果、阪神11Rは19点中18点が同じ7番絡み、16頭中5頭しか買っていない。妙味87.5で降臨した鳳の買い目も他3人とほぼ重複。運用者の言：「今回の感じの鳳さんならわざわざ出現させる必要ゼロ」「なるほどねーと唸らせる予想を出して欲しい」。加えて実データ検証では、実際の上位3頭の順位和は p−q が4指標中**最下位**（63）、EV（p×odds）は荒れた阪神で最良（24）だが堅い中山では劣る（29）＝**どちらが正しいか決められるだけの根拠がまだ無い** | 🔵**キャラごとに「見方」を分ける（2026-09-19・運用者承認）**。`config/cards.json` の characters[] に2つの上書きを追加：`objective`（歪みの測り方 "pq"＝p−q／"ev"＝p×odds−1）と `score_weights`（①②③の配分）。ケイ＝時計重視×p−q／哲さん＝共通配分×p−q／源さん＝適性重視×EV／鳳＝適性重視・人をほぼ捨てる×EV。**どれも「正解」として入れていない。4人を別々の仮説にして競わせ、成績が溜まってから決める**（20〜30レースで較正）。印（marks）は共通のままで、変わるのは買い目の選定順だけ | 反省会 2026-09-19 |
| B-8 | **鳳の賭け金** | 降臨時のみ `total=1000`（他キャラの倍）。初降臨（2026-09-19 阪神11R・妙味87.5）は他3人とほぼ同じ買い目のまま倍額を張って全外れ | 🔵**500円に引き下げ（2026-09-19・運用者承認）**。「他と違う予想を出せている」と成績で確認できるまで他キャラと同額にする。B-9 の分岐が効いて鳳が独自の買い目を出し、かつ回収率で裏づけが取れたら1000に戻す。`config/cards.json` の `characters.otori.total` と `logic/cards.py` の `CARD_TEMPLATES["otori"]` を**両方**直すこと（合計が一致しないと生成時に ValueError） | 反省会 2026-09-19 |
| B-7 | **λが小さいキャラでも軸が◎から入れ替わりうる**（★2026-09-19 の本番データでも発生：阪神11Rでケイの軸が ○ハクサンアイリス 12.8倍 になり、◎ニューオーリンズ 3.2倍 が軸から外れた） | `sel = (1−λ)×base_rank_norm + λ×myomi_rank_norm` は順位の正規化値で比較するため、◎と○のスコアが僅差で◎が大きく人気を被っている場合、ケイ（λ=0.15）でも軸が○に移ることがある（サンプルraw で実際に発生：◎の sel 0.867 vs ○ 0.872） | ✅**現状維持で確定（2026-09-19・運用者判断）**。期待値の観点で妥当な挙動なので変えない。運用者の言：「キャラ設定重視で判断すると、分析がどんどん根拠のないものになっていきそう。キャラはあくまでおまけで、3択それぞれに個性・傾向が粒立っていればいい」。→ **数字の根拠をキャラ設定に合わせて曲げない**ことを本プロジェクトの方針とする。変えたくなった場合は `config/cards.json` の kei に `axis_base_rank_floor: 1` を足せば◎固定になる（源さんが既に使っている仕組み・§5.2）| 買い目§5.1・§6 |

---

## C. 未検証のまま残っている技術的前提

| # | 内容 | 出典 |
|---|---|---|
| C-1 | ~~Cookie無し本番疎通が未検証~~ → ✅**2026-09-19 の初回本番実行で判明**。`race.netkeiba.com`（A・B）と `db.sp.netkeiba.com`（D・E）は**Cookie無しで通る**。一方 **`db.netkeiba.com`（C＝馬の戦績）は通らない**：HTTPは200だがパーサーが空を返す。同じ馬の同じページをSafariで保存したものは**現行パーサーで5走とも完全に取れる**ので、**コードの問題ではなくnetkeiba側がbotと判定して別の中身を返している**。UAを詐称して回避するのは規約に反し、ブラックリストは解除されない（実運用でのリスクが取り返しつかない）。→ **C-9 の代替経路に切り替える** | v4§2.3 / v6§3-3 |
| C-9 | ~~Cの代替経路~~ → ✅**実装・本番で稼働（2026-09-19）**。`scraper/fetchers/c2_shutuba_past.py`。`race.netkeiba.com/race/shutuba_past.html?race_id=…` から全出走馬の過去5走を1ページで取る。本番で **30頭中30頭** 取得できた。単勝オッズ・人気も同じページに載るのでB2の保険にもなる（B2の値がある場合は上書きしない）。**休養明けの馬は `td.Rest` が5走目の枠を潰すため4走**になる（実サンプルで16頭中4頭）。検証は、同じ馬の `db.netkeiba.com` 側ページを `c_horse_history` で読んだ結果との全項目一致で固定してある | 初回本番実行 |
| C-2 | Aページのグレードアイコン番号（5,16,17,18…）とグレードの対応が未確認。現状 `grade=None` + `grade_icon_raw` に生値温存。※同梱サンプル10件は「5=OP / 16=3勝 / 17=2勝 / 18=1勝」で矛盾しないが**未検証の仮説**。なおメインレースのgradeはB出馬表側が取れるので、B-2の決め方次第では実害が小さい | v6§2.2・§3-1 |
| C-3 | ~~Aページのハンデ戦判定は推定。検証待ち~~ → ✅**外れていた。2026-09-22 に解消**。実データで**4レース全部が「ハンデ」**になっていた（オールカマーGII・道頓堀S を含む）。斤量の分布が動かぬ証拠で、オールカマーは 57×9 / 55×2 / 56×1 / 58×1、道頓堀Sは 56×12 / 58×4 ＝ どちらも別定・定量の形であってハンデ戦の刻みではない。原因は2つ重なっていた：①**B（出馬表 RaceData02）は「ハンデ」を含むかだけを見ており**、別定戦では `note=None` を返していた。②`build_raw` が「Bがnull＝未取得」とみなしてAのアイコン推定で上書きしていたため、**Bが正しく「別定」と読んだ答えが毎回捨てられていた**。→ Bを4語（ハンデ/別定/定量/馬齢）の語彙で読むようにし、`build_raw` は note について**Bだけを信じる**（AとBが食い違ったらWARNINGを残す）。なお `note` は表示専用（`adapter.js`）でスコア計算には入っていないので、影響は表示のみ | v6§3-2 / 2026-09-22 |
| C-4 | ~~Cページの週内キャッシュはプロセス内のみ。土日別実行だと日曜に同じ馬を再取得する~~ → ✅**解消**。`build_raw.load_week_cache()` が**既にコミット済みの raw/{week_id}.json から past_runs を読み直す**ことで、実行をまたいだキャッシュにした（新しいファイルは作らない）。日曜ぶんの結果は既存 raw にマージされる（`merge_races`） | v5§3-2 / v6§1 |
| C-5 | `base_times.json` の生成元ページが仕様側でも未確定だった件。→ **スクリプト自体は実装済み**（`scripts/build_base_times.py`。収集経路を3つ用意：①コミット済み raw の past_runs から `finish==1` の走を拾う／②手元のJSON・CSV／③netkeibaのレース検索 `?pid=race_search_detail`）。**経路③は実サンプルで確定済み**：`?pid=race_search_detail` は存在せず（v7§2.1 の `?pid=jockey_leading` と同じ赤字応答）、**netkeibaのDBは pid形式からパス形式に作り替えられている**。検索フォームは `https://db.netkeiba.com/race/search_detail.html`、その送信先は **`GET https://db.netkeiba.com/race/list.html`**（POSTではない）。フィールドは `track[]`（1=芝 2=ダ 3=障）/ `jyo[]`（01札幌〜10小倉、30以降は地方）/ `kf`・`kt`（距離の下限・上限）/ `yf`・`mf`・`yt`・`mt`（期間）/ `baba[]` / `class[]`（1=G1…10=未勝利 11=未出走）/ `jyoken[]` / `barei[]` / `sort`（date-desc等）/ `limit`（20/50/100）。全表は `scripts/build_base_times.py` の冒頭に記録。**唯一の想定値はページ送りの `page`**（フォームに現れないため）で、効かなかった場合に同じページを積まないよう新規0件で打ち切る実装にしてある。**2026-09-18 に本番で疎通確認済み：クエリは通り、実際の結果表が返ってきた。** ただし結果表は想定と形が違い、**1行＝1レース**で着順の列が無く、代わりに「勝ち馬」「タイム」が載る（＝全行が勝ちタイム）。実ヘッダーは 開催日/開催/天気/R/レース名/映像/距離/頭数/馬場/タイム/ペース/勝ち馬/騎手/調教師/2着馬/3着馬。ヘッダー名で列を解決する作りにしていたため、誤読せず例外で止まってこの構造が分かった。パーサーは両形式に対応済み（着順の列があればそれで1着に絞る）。結果テーブルのパーサーは**ヘッダー名で列を解決**するので列構成が変わっても静かにはズレない（必要な列が無ければ例外）。疎通確認は「実在するコースで0件が返らないこと」で行う。なお `config/base_times.json` が空である限り①は全馬 null（警告ログあり） | 速度§4 |
| C-6 | Cページのパーサーは列インデックス直指定（18=タイム, 27=上り）。間にnetkeibaの有料指数列が挟まっており、列構成が変わると静かにズレる。ヘッダー名からの列解決に変えると堅くなる | （コードレビューでの指摘） |
| C-7 | **D/Eのリーディングに着度数が無い**。載るのは勝利数・勝率・連対率だけなので、`starts`/`seconds` は率から逆算している（丸め誤差は分母数百に対し1%未満）。**複勝率は `sort_key=win` のレスポンスに載らない**ため `thirds=None` になり、③人的スコアは買い目生成仕様§1.5 が定める複勝率ではなく**連対率**で評価している。`sort_key=avt`（複勝率順）のレスポンスに複勝率が載るなら解消できる（要サンプル1件） | 実サンプル解析 |
| C-10 | ~~B2オッズが全馬null~~ → ✅**解決（2026-09-19）**。発売前でも時刻の問題でもなく、原因は2段階だった。①リクエストの `type=all`・`sort` 未送信（netkeiba自身の `jquery.odds_update.js` の `_getOdds()` は `type=1`・`sort=odds` を送っていた）②レスポンスの読み違い：**馬番は配列の中ではなくキー側**（`odds["1"] = {"01": ["40.5", 0, 12], …}`）で、4要素だと思って `arr[3]` を馬番として読み IndexError で全件スキップしていた。実レスポンスの形をフィクスチャにしたテストを4件追加して固定済み | 初回本番実行 |
| C-11 | ~~式別オッズ（type=4馬連 / 5ワイド / 7 3連複）のレスポンス構造が未検証~~ → ✅**2026-09-20 の本番実行で疎通確認**。組番キーは単勝と同じ2桁ゼロ埋め連結（"0209"→"2-9"）、配列の先頭が倍率、ワイドは [下限, 上限]。ChatGPTの推測どおりだった。ただし**全組は返らない**：ワイドは全組揃うが、馬連は 114/120・70/78、3連複は 305/560・159/286 で、**人気薄の組ほど欠ける**（発売直後だから形成されていないのか、APIの打ち切りかは未判別）。これは鳳に構造的に不利で、鳳は人気薄を買うので**鳳の買い目ほど実オッズが無い**（実際どちらのレースも coverage 0.8＝5点中1点が未提供で降臨できず）。次の実行で発走直前の coverage を見て判断する | 2026-09-20 本番 |
| C-12 | **モデルの組み合わせ確率が市場と桁違いにズレる（較正の問題）** | 式別オッズが取れたので全組で突き合わせられた。**レース全体ではモデル/市場の比の中央値 1.15〜1.35倍**（だいたい市場と同じ見方）だが、**比の範囲は 0.08〜10.9倍**と極端に広い。そして鳳の目的関数 p×odds は**この比の最大値を取りに行く**ので、構造的に「旨みが大きい組」ではなく**「モデルの誤差が大きい組」**を選ぶ（2026-09-20 の鳳の5点はすべて 4〜16倍の側）。結果、カードEVは期待回収率4〜5倍＝**+300〜400%のエッジ**という、パリミュチュエル市場ではあり得ない値になる | 🔵**EVの大きさを信用せず、符号だけ使う（2026-09-20）**。`config/myomi.json` の `card_ev.mode` を `"veto"` にした：表示と降臨判定は旧メーター（モデル-市場乖離）で行い、カードEVは「5点すべてに実オッズがあり、合計EVが元本割れしていない」という**拒否権としてだけ**使う。較正（Brier/信頼度曲線、softmaxの温度Tの見直し、Harvilleの過大評価補正）が済んだら `mode: "meter"` に切り替えるとChatGPTの設計どおりカードEVの大きさで測る。**これが今いちばん大きな数学的課題**（ChatGPT handoff §5-1 も同じ結論） | 2026-09-20 本番 |
| C-8 | Dのリーディングは **`limit` が効かず1ページ20件固定**。既定5ページ＝上位100人までを取得し、表に載らない騎手は `jockey_stats=None`（③欠損）になる。メインレースに乗るのは上位騎手が中心という前提での割り切り | 実サンプル解析 |

---

## E. 運用の欠陥として直したもの（2026-09-19 の反省会より）

| # | 何が起きていたか | 直し方 |
|---|---|---|
| E-1 | **結果を見たあとの予想を採点していた** | `data/predictions.json` は実行のたびに上書きされる。初実戦の日、17:42 JST（全レース終了後）のパイプラインが確定オッズで予想を作り直し、成績集計はそれを採点していた。実際に妙味 78.2→87.5 に跳ね上がって**鳳が後付けで降臨**し、中山の哲さんの買い目も `9-14`→`9-13` に変わっていた。この状態では回収率も的中率も意味を持たない（後出しじゃんけんの成績） | **発走前予想の凍結**（`logic/snapshots.py`）。レースごとに `data/snapshots/{race_id}.json` へ「発走時刻より前に観測した最後の予想」を固め、**発走後のビルドは既存のスナップショットに一切触らない**。成績集計は predictions.json ではなくスナップショットだけを採点する（`pre_race=false` のものは集計から除外）。2026-W38 の2レースは、発走前最後のビルド（14:14 JST・commit `e2e9603`）まで遡って凍結し直した。`tests/test_snapshots.py` が「発走後のビルドは過去を書き換えられない」を固定 |
| E-3 | **50円・150円の買い目はJRAで買えない** | `amt_unit=50` は500円を厚みつきで配る自由度として入れたが（旧B-4）、JRAの発売単位は100円なので、提示した買い目をそのまま買えない。サンプルにあった `amt:150` も同じく買えない | ✅**100円単位に固定（ChatGPT PR #1 / 2026-09-20）**。哲さん・鳳のテンプレを100円の倍数に組み直した（合計500円は据え置き）。**B-4はこれで決着**（「サンプルの表現を正とする」という判断が、そもそも買えない券を正としていた） |
| E-4 | **キャラ固有の重みが順位にしか効いていなかった** | 自分（Claude）がB-9で入れたキャラ別 `score_weights` は**実力の並べ替えにしか使われず**、EV計算に使う p は全キャラ共通の45/30/25モデルのままだった。「適性を55%重視する源さん」が、期待値だけは他人のモデルの確率で計算していた状態 | ✅**キャラ固有 score → score → p → EV まで一貫（ChatGPT PR #1）**。`assign_character_ranks` がキャラ固有スコアから softmax を取り直し、`sel_p` としてHarville確率にも使う。カードに `probability_model`（温度・重み）を記録して後から再現できるようにした |
| E-5 | **鳳の降臨理由と鳳の買い目が無関係だった** | 旧妙味は「レース内のどこかに高単勝EVの馬がいる」で上がるが、鳳は別の基準で馬を選ぶ。**降臨の原因になった馬を鳳が1点も買っていない**という状態が成立していた（初実戦がまさにこれ） | 🔵**鳳が実際に買うカードを実市場オッズ（馬連=type4／ワイド=5／3連複=7）で評価し、そのEVで降臨を決める（ChatGPT PR #1）**。1点でも実オッズが欠けたカードは「測れなかった」として降臨させない（fail-closed）。**統合時の修正**：ChatGPT版は測れないと妙味を0にしていたため、レスポンス構造の推測が外れると全レースの妙味が0で並ぶ。「旨みが無い」と「測れなかった」は別物なので、**表示は旧メーターへ退避し、降臨だけを閉じたまま**にした（`myomi.resolve_myomi`・`myomi_source` で区別）。⬜ **type=4/5/7 の実レスポンス構造は未検証**（C-11） |
| E-6 | **基準タイム表が疎なせいで、データが「中途半端にある」馬だけが減点されていた** | `base_times.json` が未充足だと、ある馬は指数化できる走が0本（＝①欠損で重みが再配分され減点なし）、別の馬は「たまたま表があるダートの惨敗だけ」が指数化される（＝①が45%の重みで大きく減点）。9/20 オールカマーのヴーレヴーがこれで、芝の好走が全部指数化できず、ダートの負けだけが残っていた。**データが無い馬より、中途半端にある馬が不利**という取得都合のバグ | 🔵**スピード指数の品質ガード（ChatGPT PR #2 / 2026-09-22 統合）**。①同じsurfaceの過去走だけを使う ②指数化できる走が2本未満の馬は①を使わない ③その条件を満たす馬がレースの50%未満なら**①を全馬一律でOFF** ④50%以上なら欠損馬を観測平均で補完して z=0（中立）にする。`speed_quality` をpredictions/スナップショットに残す。**統合時の修正3点**：(a) ①を捨てたのに `n_usable` が残り、妙味の信頼度confだけ高いままだった（13頭中5頭の例でconf=0.692／本来は下限0.5）→ 捨てた走はconfからも外す (b) 平均補完がz標準化のsdを縮め、**観測できている馬のzを最大1.36倍に増幅**していた（直そうとした非対称と逆向きの歪み）→ 統計量は観測馬だけで計算し、補完馬をちょうどz=0に落とす (c) `tests/test_speed_guard.py` に `sys.path` 挿入が無いまま `run_pipeline.yml` が単体スクリプトとして呼んでおり、**次の本番実行がテスト段階で落ちる状態**だった → 修正 | 反省会 2026-09-21 |
| E-2 | **モデルの成績を比べる相手がいなかった** | 収支だけ見ても「買わなければ0円」としか比べられず、**市場に勝てているか**が判らない | **市場ベンチマークの併記**（`results/build_results.py::market_benchmark`）。各レースで「1番人気−2番人気のワイド1点を同額買う」という思考ゼロの基準戦略の収支を記録し、`summarize()["market"]` に出す。**初実戦の結果：モデル −3,000円（回収率0%）／ベンチマーク +400円（回収率140%）**。中山は◎13と△7が2・3着に来ていたのに、モデルはこの2頭を組み合わせた買い目を1点も持っていなかった（ベンチマークはそこを拾って1,400円） |

---

## D. 決着済み（参考・蒸し返さないため）

- ④オッズ乖離は合成スコアに入れない（循環回避）。妙味と買い目選定(value=p−q)に回す — v3§2.2
- 過去走は直近5走 / 式別オッズはMVP非取得（Harville近似）/ 血統はMVPスコープ外 / 騎手・厩舎はリーディング一括方式 — v2§2
- GitHub Actionsは土曜・日曜で別々に実行 — v6§1
- 騎手の**個別**成績ページ（`db.netkeiba.com/jockey/{id}/`）と騎手TOPは使わない（調査済みのハズレ） — v7§2.1
- **ハズレページの見分け方**：`db.netkeiba.com/?pid=xxx` が存在しないpidのとき、本文が
  `<span style="color:red"><b><br>URL: /?pid=xxx<br></b></span>` だけの応答になる。
  これを見たらUAやCookieを疑う前に**pidそのものが無い**と判断してよい。
  確認済み：`jockey_leading`（v7§2.1）、`race_search_detail`（C-5）
- **db.netkeiba.com は `?pid=xxx` からパス形式に移行済み**（DBトップの実サンプルで確認）。
  ネット上の旧いスクレイピング記事が挙げるpidは基本的に**もう無い**と思ってよい。
  各詳細検索の現行URL：
  `/race/search_detail.html`（レース）、`/horse/search_detail.html`（競走馬）、
  `/jockey/search_detail.html`（騎手）、`/trainer/search_detail.html`（調教師）、
  `/owner/search_detail.html`（馬主）、`/breeder/search_detail.html`（生産者）。
  キーワード検索だけなら `/horse/search_all.html`（POST、`word` と `match`）。
  **検索フォームの送信先は別URL**：レース詳細検索の場合 `/race/list.html`（GET）
- **レコードタイムの記事は基準タイムに使えない** — `dir.netkeiba.com/keibamatome/detail.html?no=2722`。
  ①レコード＝分布の最速端であり中央値の代用にならない（しかもコースごとにレコードの硬さが違うので、
  コースごとに違う量のバイアスが入る）②主要コースのみで101コースを賄えない
  ③本文は空で中身は `ajax_parts_view.html?no={4452..4463}` から後入れ（表は保存HTMLに入らない）
