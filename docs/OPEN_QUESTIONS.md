# 未決事項リスト（実装前に判断が要るもの）

引き継ぎ書v1〜v7と各仕様書に散在している「未確定」「要確認」「未反映パッチ」を1箇所に集約したもの。
判断が済んだらこの表を更新し、該当仕様書にも反映すること。

出典表記：`v3§3` = 引き継ぎ書v3 §3、`買い目§5.5` = 買い目生成仕様 §5.5、など。

---

## A. 仕様書に未反映の決定 → ✅ 全件解消（2026-09-17）

| # | 対象 | 内容 | 状態 | 出典 |
|---|---|---|---|---|
| A-1 | データスキーマ races[] | `myomi_parts`（オプション）追加：`{umami, conf}` | ✅**反映済**。`データスキーマ仕様_v1.2.md` として発行（§1・§3・§6.6）。`predictions.sample.json` にも追加 | v3§3-1 / v6§6 |
| A-2 | 妙味メーター §7 #4・#5 | 頭数は入れない／`myomi_parts` は持つ | ✅**反映済**。「要相談」→「確定」に更新し、理由も本文に記載 | v3§2.1 / v6§6 |
| A-3 | 引き継ぎ書v1 §5 の④合成 | ④オッズ乖離は base_score に入れない（v3§2.2で上書き） | ✅**対応済**。v1は歴史的資料として据え置き、冒頭に「§5はv3で上書き済み」の注記を追加 | v3§3-3 |

> コードが参照していた `データスキーマ仕様_v1_2.md` / `妙味メーター仕様_v1_確定版.md` という
> 存在しないファイル名の問題も、A-1・A-2 の反映と参照名の統一で解消済み。

---

## B. 実装の判断が要る設計論点

**凡例**：🔵＝logic実装時に判断を入れた（configで切り替え可能。後方検証で見直す前提）／⬜＝未決。

| # | 論点 | 内容 | 状態 | 出典 |
|---|---|---|---|---|
| B-1 | **スピード指数のクラス補正の向き** | `expected_time = base_time + class_sec` により「指数100＝自分のクラスのpar」になり、§1の「指数100＝OPクラス勝ち馬水準」という定義と食い違う。結果、**下級条件を遅く走った馬が上級条件を速く走った馬より高い指数になる**（京都芝1200・base 67.8 で、未勝利69.5秒＝96.7 > OP 68.3秒＝91.7）。§1.4の検算はOP戦のためこの項を検証していない | 🔵**`class_credit` を新設（既定 0.0＝クラス補正を足し戻さない＝絶対時計で評価）**。`1.0` にすれば仕様書§1の記述そのままに戻る。どちらが回収率に効くかは後方検証で決める。`tests/test_logic.py::test_speed_class_credit_controls_class_handicap` が両方の挙動を固定している | 速度§1・§4 |
| B-2 | **メインレースの絞り込みルール** | `a_race_list.py` は「絞り込みなし・全レース対象」、`build_raw.py` のTODOは「絞り込みルール未確定」と食い違う。仕様側は一貫して「メインのみ」（取得項目§2.2＝土日のメイン4〜6、速度§0＝「現在の取得設計（メインレースのみ）」） | 🔵**A単体は絞り込み無し・絞り込みは build_raw の責務**と整理し、`config/scraper.json` の `main_race` で切り替えられるようにした（`race_no`（既定・各場11R）/ `grade` / `all`）。既定の11Rは3場開催の土日で6レースとなり、§2.2の「4〜6」と一致する。**「メイン」の定義そのものは運用者の確認待ち**（例：メインが10Rや12Rの日をどう扱うか） | 取得§1.1・§2.2 / 速度§0 |
| B-3 | **騎手リーディングの場別スコープ** | 取得項目§2.6 は `scope="venue"`（当該場成績）と定めるが、リーディングページが場別絞り込みに対応しているか未確認。非対応なら ①場別URL探索 ②overallに落とす（仕様変更）③詳細検索を叩く の選択。推奨は ①→③→② | ⬜**未決**（D実装時）。②を選ぶ場合は共通内部フォーマット §2.6 の改訂が必要（黙って値だけ変えないこと） | v7§2.4 |
| B-4 | **`amt` の単位** | 買い目§5.5 不変条件#1は「すべて100円単位」だが、`predictions.sample.json` と `results.sample.json` の哲さんカードに `amt: 150` がある（Σ=500 なので#2は満たす） | 🔵**`config/cards.json` の `amt_unit` を新設（既定 50）**。500円を厚み付きで配分する自由度を優先し、サンプルの表現を正とした。`100` に戻せば仕様の記述どおり | 買い目§5.5 |
| B-5 | **`marks[]` の範囲** | 買い目§2 は「7位以下は無印（marksに載せるが mk 空）」＝全出走馬。サンプルの marks は上位5頭のみ | 🔵**仕様の記述どおり全出走馬を載せる**（7位以下は `mk: ""`）。データスキーマ§3が「marks の score と odds は後方検証のために残す」と定めており、上位数頭だけでは重みの再計算ができないため。UIは `mk` が空の行を描画しなければよい | 買い目§2・§5.5 |
| B-6 | **softmax温度Tとスコアのスケール** | 妙味§1 の T=10.0 は、§4の検算（score 60〜90）と `marks[].score`（81.2 等）が示すとおり**0〜100スケールのスコア前提の値**。一方 base_score は z の加重和（概ね ±2）なので、そのまま T=10 で割ると全馬ほぼ横並びの p になり妙味が常時振り切れる。妙味§7 #2 が「Tは仮値、スコアスケール確定後に較正」と予告していた箇所 | 🔵**Tを動かさず、z を表示スケールに移すアフィン変換を新設**：`score = score_scale.base + score_scale.unit × base_score`（既定 70 + 10×z）。`score_unit`(10) と T(10) が等しいので実効的には「z空間で温度1.0」。18頭立てなら1番人気の p は25%前後に収まる | 妙味§1・§7#2 |
| B-7 | **λが小さいキャラでも軸が◎から入れ替わりうる** | `sel = (1−λ)×base_rank_norm + λ×myomi_rank_norm` は順位の正規化値で比較するため、◎と○のスコアが僅差で◎が大きく人気を被っている場合、ケイ（λ=0.15）でも軸が○に移ることがある（サンプルraw で実際に発生：◎の sel 0.867 vs ○ 0.872） | ⬜**様子見**。期待値の観点では「最も人気を被った馬を軸にしない」挙動は妥当で、λ=0.15 が非ゼロである以上は仕様どおり。ケイの軸を必ず◎に固定したいなら λ=0 にするか、軸だけ base_rank 順で決める改修が要る | 買い目§5.1・§6 |

---

## C. 未検証のまま残っている技術的前提

| # | 内容 | 出典 |
|---|---|---|
| C-1 | B2オッズAPI・Aの2エンドポイント（date_list / race_list_sub）の **Cookie無し本番疎通が未検証**。実装では `X-Requested-With` + `Referer` のみ。弾かれたらCookie取得ステップの追加を検討 | v4§2.3 / v6§3-3 |
| C-2 | Aページのグレードアイコン番号（5,16,17,18…）とグレードの対応が未確認。現状 `grade=None` + `grade_icon_raw` に生値温存。※同梱サンプル10件は「5=OP / 16=3勝 / 17=2勝 / 18=1勝」で矛盾しないが**未検証の仮説**。なおメインレースのgradeはB出馬表側が取れるので、B-2の決め方次第では実害が小さい | v6§2.2・§3-1 |
| C-3 | Aページのハンデ戦判定（`Icon_GradeType13` の有無で `note="ハンデ"`）は実データパターンからの推定。複数週での検証待ち | v6§3-2 |
| C-4 | ~~Cページの週内キャッシュはプロセス内のみ。土日別実行だと日曜に同じ馬を再取得する~~ → ✅**解消**。`build_raw.load_week_cache()` が**既にコミット済みの raw/{week_id}.json から past_runs を読み直す**ことで、実行をまたいだキャッシュにした（新しいファイルは作らない）。日曜ぶんの結果は既存 raw にマージされる（`merge_races`） | v5§3-2 / v6§1 |
| C-5 | `base_times.json` の生成元ページ（過去3年の勝ちタイム一覧をどこから取るか）が仕様側でも未確定。**空のままだと usable判定#4 で全走が落ち、エラーにならず静かに speed=None になる**ので、実装時はログを出すこと | 速度§4 |
| C-6 | Cページのパーサーは列インデックス直指定（18=タイム, 27=上り）。間にnetkeibaの有料指数列が挟まっており、列構成が変わると静かにズレる。ヘッダー名からの列解決に変えると堅くなる | （コードレビューでの指摘） |

---

## D. 決着済み（参考・蒸し返さないため）

- ④オッズ乖離は合成スコアに入れない（循環回避）。妙味と買い目選定(value=p−q)に回す — v3§2.2
- 過去走は直近5走 / 式別オッズはMVP非取得（Harville近似）/ 血統はMVPスコープ外 / 騎手・厩舎はリーディング一括方式 — v2§2
- GitHub Actionsは土曜・日曜で別々に実行 — v6§1
- 騎手の**個別**成績ページ（`db.netkeiba.com/jockey/{id}/`）と騎手TOPは使わない（調査済みのハズレ） — v7§2.1
