# 競馬予想アプリ 引き継ぎ書 v9（D/E/F 実装完了・フェッチャー全揃い）

v8 からの**差分のみ**を記録する。実装済み内容・仕様の本体は各仕様書と v8 を参照すること。

**本書のスコープ：残っていた D（騎手リーディング）・E（調教師リーディング）・F（結果・払戻）を
実装し、A〜F すべてのフェッチャーが揃った状態。** 残る未実装は `scripts/build_base_times.py` のみ。

---

## 0. まず新しいセッションで伝えること（コピペ用）

> 競馬予想アプリ「週末3CARDS」の続きです。成果物は GitHub の `reiji55/Predict-horse-race`
> （ブランチ `claude/content-review-full-rw32o2`）に全部入っています。
> まず README.md と docs/OPEN_QUESTIONS.md を読んでください。
> **A〜F の全フェッチャー・logic・成績集計・UIのデータ接続まで実装済み**で、
> 残りは基準タイム表（scripts/build_base_times.py）と本番での疎通確認です。
> 私は要望とニュアンス主体で話すので、認識のズレ・データ不足・誤認識は都度指摘してください。

---

## 1. プロジェクトの現在地

| # | フェッチャー | 状態 |
|---|---|---|
| A | 開催日別レース一覧 | ✅実装・検証済み |
| B | 出馬表 | ✅実装・検証済み（**v9で府中牝馬Sの実サンプルを再入手し検証し直した**） |
| B2 | オッズ（AJAX API） | ✅実装済み（オッズAPIの生レスポンスのサンプルは未入手） |
| C | 馬の戦績 | ✅実装・検証済み |
| D | 騎手リーディング | ✅**v9で実装**（実サンプルで検証） |
| E | 調教師リーディング | ✅**v9で実装**（Dと同一API・category違い） |
| F | レース結果・払戻 | ✅**v9で実装**（実サンプルで検証） |

| 層 | 状態 |
|---|---|
| `scraper/build_raw.py` | ✅実装済み |
| `logic/` 全7モジュール | ✅実装済み（仕様書の検算サンプルでテスト済み） |
| `results/build_results.py` | ✅実装済み（Fと接続済み） |
| `scripts/build_base_times.py` | ⬜**未実装**（残る唯一の未実装。取得元ページが仕様側でも未確定） |
| UI（モックv7） | ✅実データ表示に対応 |
| GitHub Actions | ✅実処理に差し替え済み |

テストは **ネットワーク非依存で69件**（実サンプルHTMLが必要なものは、無ければ自動でスキップ）。

---

## 2. D・E で分かったこと（実サンプル解析）

### 2.1 APIの特定

リーディングページも A・B2 と同じ「静的HTMLは空、AJAXが後から描画」パターン。
ページ内のインラインJSに呼び出しパラメータがそのまま書かれており、
`org_dbapi.action.js` の `getLeadingData()` が下記を投げていた：

```
GET https://db.sp.netkeiba.com/
    ?pid=api_get_leading&input=UTF-8&output=json
    &limit=20&category={jockey|trainer}&page=1&show=leading_detail
    &bel=&year=2026&sort_key=win
```

- **D と E は `category` が違うだけの同一API** → `scraper/common/leading_api.py` に共通化
- レスポンスは **HTML断片をJSON文字列で包んだもの**（`output=json` でも生JSONではない）
- v7 §2.2 で見つけていた `ajax_leading_box.html` は別系統（サイドバーの要約box）。本命はこちら

### 2.2 ★ jockey_ref の桁合わせ（v7 §2.5 の要確認事項）→ 解消

レスポンスの騎手リンクは `db.sp.netkeiba.com/jockey/01174/` の形で、**5桁ゼロ埋め**。
B出馬表で取得している `jockey_ref` と同形式であることを確認した（Ｃ.ルメール＝05339）。
振り分けが壊れる懸念は無くなった。

### 2.3 ⚠ 着度数が載っていない（OPEN_QUESTIONS C-7）

レスポンスに入るのは **勝利数・勝率・連対率** だけで、取得項目仕様§2.6 が求める着度数は無い。
そこで率から逆算している：

```
starts  = wins / 勝率
seconds = starts × 連対率 − wins
thirds  = starts × 複勝率 − wins − seconds   ← 複勝率が載っていれば
```

率は小数第1位までなので starts に丸め誤差が乗るが、分母が数百なので相対誤差1%未満。
③人的スコアは縮小推定を通すため実用上問題ない。

**ただし複勝率は `sort_key=win` のレスポンスに載らない**ため、現状 `thirds=None`。
③は買い目生成仕様§1.5 が定める複勝率ではなく **連対率** で評価している
（全騎手が同じ指標で揃うので、レース内z標準化の相対評価としては同等に機能する）。
→ **`sort_key=avt`（複勝率順）のレスポンスを1件もらえれば解消できる**。

### 2.4 ⚠ limit が効かない（C-8）

`limit=100` を指定しても20件しか返らない（サーバー側で固定）。
既定で5ページ＝**上位100人**まで取得し、表に載らない騎手は `jockey_stats=None`（③欠損）。
メインレースに乗るのは上位騎手が中心という前提での割り切り。

### 2.5 場別リーディングは存在しない（v8 から継続・確定）

PC版 `?pid=jockey_leading` は実体が無く、sp版の絞り込みは 全国/関東/関西＝**所属**のみ。
取得項目仕様を **v1.1** に改訂して騎手も `scope="overall"` にした（`venue` フィールドは廃止）。

---

## 3. F で分かったこと（実サンプル解析）

**このページは素のHTMLに中身が入っている**（AJAXではない）。2026-09-13 中山1R で検証。

- **着順**：`table#All_Result_Table` の 0=着順 / 2=馬番。中止・除外の行は除外する
- **払戻**：`table.Payout_Detail_Table` の行class で券種を判別
  （`Wide` / `Umaren` / `Fuku3` が本アプリの3券種。単勝・複勝・枠連・馬単・3連単も同じ構造）
  - 組み合わせは **`<ul>` 1つ＝1組**（空の `<li>` は桁合わせの埋め草）
  - 配当は `td.Payout` のテキストを **`<br>` で分割**して組の順に対応させる
  - この構造なので、**ワイドの3組も同着の複数配当もそのまま正しく取れる**
- Fの戻り値は `results.build_results.build_results()` にそのまま渡せる形にしてある

### 【発見】1日分の払戻をまとめて取れるページ

`race.netkeiba.com/top/payback_list.html` は**その日の全レースの払戻表**を1ページに持つ
（実サンプルでは12レース分＋各レースの race_id リンク）。ただし**着順が載っていない**ので
results.json には使えない。過去分を一括で埋めたくなったときの選択肢として記録しておく。

---

## 4. 次にやること

| 優先 | 内容 | 必要なもの |
|---|---|---|
| 1 | **`scripts/build_base_times.py`**（基準タイム表の構築） | 過去3年の勝ちタイムを**一覧で**取れるページの実サンプル。コース指定のレース検索結果（`db.netkeiba.com/?pid=race_search_detail` 系）が候補。**これが入らないと①スピード指数が丸ごと効かない** |
| 2 | **本番での疎通確認**（C-1） | 実際にActionsを1回走らせる。Cookie無しでAJAX系APIが通るかは未検証のまま |
| 3 | ③の複勝率化（C-7） | `sort_key=avt` のAPIレスポンス1件 |
| 4 | B2オッズAPIのテスト復旧 | オッズAPIの生レスポンス1件（実装自体はv4で検証済み） |
| 5 | PWAの仕上げ | GitHub Pages の有効化、manifest / Service Worker |

> **1が入って初めて①②③が揃う**。現状は②適性と③人的（連対率ベース）だけが効く状態で、
> ①は `config/base_times.json` が空のため全馬 null（エラーにはならず警告ログが出る）。

---

## 5. 進め方のルール（v3から継続・維持）

- **モデル**：実装は Sonnet で十分。設計・仕様整合チェックは Opus
- **対話スタイル**：運用者は要望・ニュアンス主体。認識のズレ・データ不足・誤認識は都度指摘
- **HTMLサンプルの渡し方**：開発環境から netkeiba はネットワークポリシーでブロックされている
  （プロキシが403）。Safariの「Webアーカイブ」保存か、ショートカットAppの
  「URLの内容を取得」→「ファイルに保存」で渡す。**APIを直接叩くものは後者が確実**
- **リクエストマナー**（共通内部フォーマット仕様§1.1）は調査中の試行錯誤でも厳守
- ギャンブル/ハウスエッジは理解済み。射幸性を煽らず正直な成績表示を重視

---

## 6. 変更履歴

| 版 | 内容 |
|---|---|
| v9 | D・E・F フェッチャーを実装（リーディングAPIの特定、着度数が無いので率から逆算、Fの払戻表の構造確定）。jockey_ref の桁合わせ（v7 §2.5）を解消。Bの実サンプルを再入手して検証し直し。これでフェッチャーが全部揃い、残る未実装は基準タイム表のみ |
| v8 | 成果物をリポジトリに統合。仕様書の未反映パッチを全件反映。logic 全7モジュール・build_raw・成績集計・UIのデータ接続を実装 |
| v7 | D調査の途中経過（ハズレページ2種、次に当たるURL候補、場別スコープのフォーク） |
| v6 | A実装・検証完了。Actions土日別実行の方針確定 |
| v5 | C実装・検証完了 |
| v4 | B・B2実装完了 |
| v3 | 設計フェーズ(1〜5)完了 |
