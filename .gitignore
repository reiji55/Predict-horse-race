__pycache__/
*.pyc
.venv/
# raw/{week_id}.json はコミットする（後方検証でロジック改良時に過去週を再計算するため）
# 実サンプルHTMLはnetkeibaの著作物。本リポジトリはパブリックなのでコミットしない。
# 配置すべきファイルと入手方法は tests/samples/README.md を参照（READMEだけは追跡する）
tests/samples/*
!tests/samples/README.md
